{
  "name": "registry-test",
  "version": "1.0.0",
  "description": "Test suite for student registry library",
  "main": "index.js",
  "scripts": {
    "pretest": "cd .. && npm run build",
    "test": "del registrardb.sqlite && mocha ./index"
  },
  "author": "David Herron <david@davidherron.com>",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^10.2.0"
  }
}
